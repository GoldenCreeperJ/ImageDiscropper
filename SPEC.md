# ImageDiscropper 功能规格（SPEC）

> 本文件是**唯一行为基线**：只保留行为契约，不含实现细节与过程性内容。
> 引擎（Core）、命令行（CLI）、桌面端（GUI）的行为与测试均以此为准；实现状态见各模块 README，
> 贡献约定见仓库根 `CONTRIBUTING.md`。

---

## 1. 定位与统一引擎

> **一句话定位**：常规裁剪只回答「保留哪一块」；本工具回答「**沿哪些线切开、留下哪些块、怎么重新拼起来**」。

| 层级     | 模式       | 定位                       | 极性                |
|--------|----------|--------------------------|-------------------|
| **L1** | 标准提取模式   | 兼容基线，降低上手门槛              | 恒 `keep`          |
| **L2** | 反向剔除模式 ⭐ | 差异化内核（按线删除、零伪造、无损）       | 恒 `remove`        |
| **L3** | 网格分割模式 ⭐ | 完备表达层（参数化网格 + 显式排序 + 重排） | `keep` 或 `remove` |

三者在数学上是包含关系（L1 ⊂ L2 ⊂ L3），在产品上并列呈现，但**共用同一条引擎代码路径**（NFR-0：模式仅为参数预设）。
所有功能归约为同一条 **Grid-Selection-Emit** 流水线：

```text
原图 → ① 切割线集合 → ② 诱导网格 → ③ 选择集 → ④ 极性 → ⑤ 排布导出
```

| 阶段     | 定义                                                                                                   |
|--------|------------------------------------------------------------------------------------------------------|
| ① 切割线  | 竖线 `X = {0 = x₀ < x₁ < … < xₘ = W}`，横线 `Y = {0 = y₀ < y₁ < … < yₙ = H}`。**贯穿全图的直线，不是线段**——这是整个工具的公理。 |
| ② 诱导网格 | `m × n` 个单元 `C[i][j] = [xᵢ, xᵢ₊₁) × [yⱼ, yⱼ₊₁)`，互不重叠且恰好铺满原图。                                         |
| ③ 选择集  | `S ⊆ {C[i][j]}`。                                                                                     |
| ④ 极性   | `keep` → 保留集 `R = S`；`remove` → `R = 全集 \ S`。                                                        |
| ⑤ 输出   | `separate`（多图分离）或 `merged`（合并单图）；合并又分 `collapse`（坍缩）与 `rearrange`（重排）。                               |

---

## 2. 术语与坐标约定

### 2.1 坐标约定

- 整数像素；区间左闭右开 `[a, b)`。
- 原点左上，`x` 向右，`y` 向下；原图尺寸 `W × H`。
- 交互浮点坐标进入引擎前统一 `round`。

### 2.2 术语表

| 术语                  | 定义                                                      |
|---------------------|---------------------------------------------------------|
| **切割线（cut line）**   | 贯穿整幅图像的水平或垂直直线，而非线段。这是本工具区别于常规裁剪的根本前提。                  |
| **带（band）**         | 相邻两条同向切割线之间的整条区域；竖带全高，横带全宽。                             |
| **单元格（cell）**       | 一条竖带与一条横带的交集，是选择与导出的最小单位。                               |
| **十字带（cross band）** | 由一个矩形选区诱导的「竖带 ∪ 横带」。                                    |
| **极性（polarity）**    | `keep` / `remove`，决定选择集是保留还是剔除。                         |
| **坍缩（collapse）**    | 删除若干整行/整列单元后，将剩余单元向原点方向紧贴拼接。                            |
| **重排（rearrange）**   | 按显式序列将保留块填入新画布，允许改变相对位置。                                |
| **选区锚点**            | 用户框选产生的矩形对角坐标 `(x1, y1, x2, y2)`，满足 `x1 < x2, y1 < y2`。 |
| **区域片段（Fragment）**  | 经切割后产生的每一个独立矩形像素块。                                      |

### 2.3 基本形状

由两条水平线和两条竖直线定义：

| 名称   | 定义                                                      |
|------|---------------------------------------------------------|
| 中心矩形 | `Rect(x1, y1, x2, y2)`                                  |
| 水平带  | `HBand(y1, y2) = Rect(0, y1, W, y2)`                    |
| 垂直带  | `VBand(x1, x2) = Rect(x1, 0, x2, H)`                    |
| 十字区域 | `Cross(x1, x2, y1, y2) = HBand(y1, y2) ∪ VBand(x1, x2)` |
| 四角区域 | `I \ Cross(x1, x2, y1, y2)`                             |

---

## 3. 功能需求

### 3.1 基础图像处理（FR-1，前置层）

作用于原图，在切割之前执行，产出图像成为切割引擎输入。

| ID     | 功能 | 要求                                                                                            |
|--------|----|-----------------------------------------------------------------------------------------------|
| FR-1.1 | 旋转 | 90° 步进（无损，仅重排像素）。                                                                             |
| FR-1.2 | 翻转 | 水平 / 垂直。                                                                                      |
| FR-1.3 | 标注 | 矩形/正方形/菱形、圆形/椭圆/扇形、三角/等腰/等边/直角、直线/箭头/多线段、文字、画笔，自定义颜色、粗细、填充等；作为**独立图层**存在，可在导出时选择是否烧录（burn-in） |
| FR-1.4 | 调整 | 缩放、图像尺寸调整、黑白、反色、色道分离、颜色选取。                                                                    |
| FR-1.5 | 控制 | 撤销、重做、清除、预览。                                                                                  |

> **设计约束**：标注图层与切割操作解耦——切割时标注随像素一起被切开，而不是被单独处理。

### 3.2 L1 · 标准提取模式（兼容基线）

| 形态   | 切割线                              | 选择集  | 结果     |
|------|----------------------------------|------|--------|
| 十字保留 | `X={0,x1,x2,W}`, `Y={0,y1,y2,H}` | 中心单元 | 常规矩形裁剪 |
| 横线保留 | `X={0,W}`, `Y={0,y1,y2,H}`       | 中间横带 | 保留一条横条 |
| 竖线保留 | `X={0,x1,x2,W}`, `Y={0,H}`       | 中间竖带 | 保留一条竖条 |

极性恒为 `keep`，输出恒为单图，无需暴露额外选项。

### 3.3 L2 · 反向剔除模式 ⭐ 差异化内核

#### 3.3.1 独特性从何而来

独特的不是「删除」，而是「**按线删除**」——这是 crop 的对偶运算：同一个选框、同一组切割线，只是极性相反。

| 维度     | 常规工具的「删除」              | 本工具的反向剔除                |
|--------|------------------------|-------------------------|
| 切割对象   | 线段围成的框                 | 贯穿全图的**直线**             |
| 结果     | 原图上留下一个洞               | 图像被真正**切成若干块**          |
| 尺寸     | 不变                     | **变小**（`W−Δx` × `H−Δy`） |
| 语义     | 遮盖 / 涂抹 / 修复           | **抽掉中缝，两侧对接**           |
| 是否需补像素 | 需要（填色或 AI 修复，必然引入伪造内容） | **完全不需要，零伪造、无损**        |

典型场景：长截图删中段、表格删行列、删水印带/页眉页脚、四角素材一次提取（传统做法需裁 4 次）。
L2 在数学上是 L3 的子集，但在产品上是独立的一等公民：1 次拖拽即完成，而网格实现同样效果需参数配置 + 多次点选。

#### 3.3.2 功能定义

| ID      | 需求                                               |
|---------|--------------------------------------------------|
| FR-L2.1 | **十字切割**：矩形选框 + `remove` → 保留左上、右上、左下、右下四块。      |
| FR-L2.2 | **横线切割**：两条横线 + `remove` → 保留上、下两块。              |
| FR-L2.3 | **竖线切割**：两条竖线 + `remove` → 保留左、右两块。              |
| FR-L2.4 | **多矩形并集剔除**：`k` 个矩形，删除区域取并集（语义见 §3.3.5）。         |
| FR-L2.5 | **极性开关**：与 L1 共用同一界面，一键在「保留框内 / 删除框内」之间切换，并实时预览。 |
| FR-L2.6 | 输出：多图分离导出 / 合并单图导出（坍缩式，见 §4.2）。                  |

单矩形十字切割的保留块（`0 ≤ x1 < x2 ≤ W`，`0 ≤ y1 < y2 ≤ H`）：

| 块  | 区域 `(左, 上, 右, 下)` | 尺寸                |
|----|-------------------|-------------------|
| 左上 | `(0, 0, x1, y1)`  | `x1 × y1`         |
| 右上 | `(x2, 0, W, y1)`  | `(W−x2) × y1`     |
| 左下 | `(0, y2, x1, H)`  | `x1 × (H−y2)`     |
| 右下 | `(x2, y2, W, H)`  | `(W−x2) × (H−y2)` |

#### 3.3.3 边界情况

1. **退化选框**：`x1 = x2` 时语义即单线切割——GUI 允许只画一条线（自动按横线/竖线切割处理）；CLI 不自动降级，`--rect` 退化直接报错并提示改用 `--hband` / `--vband`。
2. **贴边选框**：`x1 = 0` 时左侧两块宽度为 0，导出时跳过空块。
3. **像素归属**：统一 `[x1, x2)`，杜绝边界像素重复或丢失。

#### 3.3.4 多矩形语义（方案 A：十字带并集）

删除区域 `D = ⋃ᵢ ( VBand(x1ᵢ, x2ᵢ) ∪ HBand(y1ᵢ, y2ᵢ) )`，输出 `Out = I \ D`。

- 与单矩形逻辑严格一致，保持「按线切割」公理；多个矩形可重叠、相邻或分离。
- 删除后保留区域可能不连通，系统应识别连通域，支持按连通域拆分导出或合并导出。
- 该方案下被删除的是若干整列和整行的并集，满足坍缩条件（见 §4.4）。
- 其他多矩形方案（如仅删除矩形内部）与「按线切割」公理冲突，不采用。

### 3.4 L3 · 网格分割模式 ⭐ 完备表达层

#### 3.4.1 高级性从何而来

| 维度    | L1 / L2    | L3                      |
|-------|------------|-------------------------|
| 切割线数量 | 固定 2 条/轴   | 任意条，参数化生成               |
| 选择集形状 | 由形态决定      | 任意子集，逐单元自由点选            |
| 单元顺序  | 隐含空间顺序     | **可编排的显式序列**            |
| 输出布局  | 只能坍缩回原相对位置 | 可重排为任意 `cols × rows` 画布 |

「顺序」是 L3 独有的第三个自由度：可做拼图打乱、条漫切分、精灵图拆解与重打包。

#### 3.4.2 功能定义

| ID      | 需求                                                                                                            |
|---------|---------------------------------------------------------------------------------------------------------------|
| FR-L3.1 | **网格定义**：基准点 `(x₀, y₀)`、单元尺寸 `(cw, ch)`；行数、列数由图像边界自动推导，不作为独立参数（详见 §3.4.3）。                                    |
| FR-L3.2 | **余量策略**：`W − x₀` 非 `cw` 整数倍时须显式选择——`discard` 丢弃 / `keep-partial` 保留残缺单元 / `pad` 补白至完整单元。                     |
| FR-L3.3 | **单元选择**：单击切换、拖拽框选、全选 / 反选 / 反相。                                                                              |
| FR-L3.4 | **极性**：`keep` 保留选中 / `remove` 剔除选中，与 L1/L2 同一套开关。                                                             |
| FR-L3.5 | **排序策略**：`row-major`（横优先）、`column-major`（竖优先），各自可叠加 `reverse`、`snake`；以及 `custom`（点选顺序即序号，单元角标实时显示编号，支持拖拽调序）。 |
| FR-L3.6 | **分离导出**：按序命名，逐张写入目标文件夹（不压缩打包）。                                                                               |
| FR-L3.7 | **重排合并导出**：按序填入用户指定的 `cols × rows` 画布，自定义单元长宽，空位与余量以指定颜色（默认透明）填充。                                             |
| FR-L3.8 | **配置保存与复用**：网格参数可存为预设，应用到其他同尺寸图像（批处理基础）。                                                                      |

UI 提供「**转为网格模式编辑**」入口：用户在 L2 画完选框后，可一键把当前诱导网格送进 L3 继续精修。

#### 3.4.3 网格定义

网格只允许通过四个几何参数定义：**基准点 `x0`、`y0`，单元宽度 `cellWidth`，单元高度 `cellHeight`**。

- `x0`、`y0` 决定网格切割线的起始位置；`cellWidth` / `cellHeight` 决定垂直/水平切割线之间的周期距离。
- 所有切割线均为贯穿整幅图像的完整水平线 / 垂直线；不存在仅在局部网格区域内生效的切割线。
- 行数、列数由图像边界与上述参数自动推导，不作为用户必须指定的独立参数。

> 因此 L3 网格的本质是：**以基准点为参考、以单元尺寸为周期生成贯穿整幅图像的水平/垂直切割线，再由这些切割线诱导出网格单元**。

---

## 4. 导出规格（三层共用）

### 4.1 分离导出

每个保留单元一张图，命名模板可配置（如 `{name}_{index:03d}` / `{name}_r{row}c{col}`），多文件逐张写入目标文件夹（不压缩打包）。

### 4.2 合并导出 · 坍缩式（L1/L2 主用）

剩余块按原相对位置紧贴拼接，结果尺寸 `(W − Δx) × (H − Δy)`。十字切割的贴图位置：

- 左上 → `(0, 0)`；右上 → `(x1, 0)`；左下 → `(0, y1)`；右下 → `(x1, y1)`

> **实现提示**：不必真的切成九块。坍缩式合并 ≡ 先删整列条带、再删整行条带，两次一维拼接的复合。代码更短、更不易错。

### 4.3 合并导出 · 重排式（L3 主用）

按 FR-L3.5 的序列填入指定画布，空位以填充色补齐。**仅 L3 网格模式支持**：
非 L3 指定重排、以及 L1/L2 不可坍缩的情形，引擎直接报错（不自动降级）。

### 4.4 关键约束：坍缩的可行性条件

> **定理**：保留集 `R` 能无空洞、无重叠地坍缩为矩形图，当且仅当被剔除的单元恰好构成诱导网格中的**若干整行和/或整列**。

- 单矩形十字切割 ✅ 满足；横线 / 竖线切割 ✅ 满足；多矩形并集剔除 ✅ 满足；L3 任意选择集 ❌ 一般不满足。

**处理策略**：不满足时，坍缩式合并自动禁用并提示原因，降级为重排式合并（空缺填充色补齐）。**不引入装箱算法**——复杂度与收益不成正比，明确列为 out of scope。

### 4.5 格式

PNG（支持透明）、JPEG（须指定背景色替代透明）、WebP、BMP 等；可选质量参数。
解码统一加载为 RGBA；合并导出的 JPEG/BMP 会把 alpha 压平到 `padColor`（未指定时为透明黑）。

---

## 5. 边界与异常（E-1 ~ E-8）

| 编号  | 情形                          | 行为                                                                              |
|-----|-----------------------------|---------------------------------------------------------------------------------|
| E-1 | 选框退化（`x1 > x2` 且 `y1 > y2`） | 禁止生成切割区域，引擎报错                                                                   |
| E-2 | 切割线超出图像边界                   | 自动裁剪到 `[0, W] × [0, H]` 并补入边界                                                   |
| E-3 | `x1 = x2` 退化选框              | 语义即单线切割：GUI 允许只画一条线（按横线/竖线切割处理）；CLI 的 `--rect` 退化直接报错，须改用 `--hband` / `--vband` |
| E-4 | 贴边选框 / 零宽单元                 | 宽度为 0 的块导出时跳过                                                                   |
| E-5 | 网格单元部分越界                    | 按余量策略 `discard` / `keep-partial` / `pad` 处理                                     |
| E-6 | 删除区域为空（`remove` 却无单元被选中剔除）  | 报错                                                                              |
| E-7 | 保留区域为空（删除区覆盖全图）             | 禁止导出                                                                            |
| E-8 | 导出路径为空 / 父目录不可创建            | 返回失败（不崩溃）；分离导出目标文件夹不存在时自动创建                                                     |

---

## 6. 非功能需求

| ID     | 需求                                                                                              |
|--------|-------------------------------------------------------------------------------------------------|
| NFR-0  | **引擎优先**：三层模式共用同一核心，模式仅为参数预设。                                                                   |
| NFR-1  | **本地处理**：图像不上传服务器。                                                                              |
| NFR-2  | **无损优先**：切割为纯像素搬运，不重编码；仅 JPEG 输出允许有损。                                                           |
| NFR-3  | **性能**：8000×8000 px 图像预览交互 ≥ 30 fps（预览用降采样副本，导出时才对原分辨率操作）。                                      |
| NFR-4  | **可逆性**：全流程撤销 / 重做；参数序列化为配置对象，可导出复用。                                                            |
| NFR-5  | **精确输入**：所有几何参数除拖拽外均支持数值直接输入。                                                                   |
| NFR-6  | **实时预览**：极性切换、排序变更须即时反映在画布上——这是让用户「看懂」反向剔除的关键。                                                  |
| NFR-7  | **吸附与微调**：支持鼠标拖拽切割线、网格线；支持键盘微调坐标；支持坐标吸附：图像边缘、网格线、中心线。                                           |
| NFR-8  | **大图处理**：支持大图分块处理，避免内存溢出。                                                                       |
| NFR-9  | **元数据**：导出时保留元数据可选。                                                                             |
| NFR-10 | **实现一致性**：任何语言重实现须与参考实现（C++）行为对齐——共享黄金测试集，同输入必须产出逐像素一致的输出、一致的退出码与错误语义；新功能在参考实现落地并通过黄金测试后方可宣称完成。 |

---

## 7. 配置 JSON schema

```json
{
  "source": { "width": 1920, "height": 1080 },
  "preprocess": [
    { "op": "rotate", "deg": 90 },
    { "op": "flip", "axis": "horizontal" }
  ],
  "cut": {
    "tier": "L2",
    "generator": "rect",
    "params": { "x1": 300, "y1": 200, "x2": 900, "y2": 700 },
    "polarity": "remove"
  },
  "order": { "strategy": "row-major", "reverse": false, "sequence": null },
  "emit": {
    "mode": "merged",
    "merge": {
      "layout": "collapse",
      "cols": null, "rows": null,
      "cellSize": null,
      "padColor": "#00000000"
    },
    "format": "png",
    "naming": "{name}_{index:03d}"
  }
}
```

配置**不含图像像素与输入/输出路径**，仅保存参数态；由各实现的配置读写接口负责保存 / 加载该 schema。

---

## 8. 三层行为契约

> 本节规定三层各自的**对外行为**——任何语言重实现、任何新前端（含移动端）均须满足。
> 实现结构（类 / 文件 / 框架机制）不属于本文件，见各模块 README。

### 8.1 CLI 行为契约（`idc`）

**命令与层级映射**：

| 命令        | 层级      | 极性                | 输出                                                    |
|-----------|---------|-------------------|-------------------------------------------------------|
| `extract` | L1 标准提取 | 恒 `keep`          | 合并单图（坍缩）                                              |
| `erase`   | L2 反向剔除 | 恒 `remove`        | 分离 / 坍缩（`--merge rearrange` 判参数错误，退出码 1）              |
| `grid`    | L3 网格分割 | `keep` 或 `remove` | 分离 / 重排合并（`--merge-sort` / `--merge-decorate` 控制填充路径） |
| `config`  | 配置驱动    | 由 JSON 决定         | 由 JSON 的 `emit` 决定                                    |

**全局选项**：`-h/--help`、`-v/--version`、`--verbose`、`--quiet`、`--config <file>`（从 JSON 读取全部参数）、
`--save-config <file>`（把当前参数序列化为 JSON，dry-run 不执行）。

**参数规则**：

- `--rect` 退化（`x1 = x2` 或 `y1 = y2`）直接报错（退出码 1），提示改用 `--hband` / `--vband`（E-3）。
- `--grid x0,y0,cw,ch`：基准点 `x0/y0` 可为负（网格从图像左上边界之外开始铺）；`cw/ch` 必须为正；
  以 `-数字` 开头的负值选项正常按值解析。
- `--canvas <cols>x<rows>` 的 cols/rows 必填且为正（`grid --compose` 与 config 的 REARRANGE 配置同此约束），不由 Core 兜底。
- 重排仅为 L3 专属：`erase`（L2）与 config 加载的非 L3 配置指定 `rearrange` 均判参数错误（退出码 1）。
- 填充顺序与选择排序正交：`--sort`（row-major / column-major / custom）决定保留块列表；
  `--merge-sort <row-major|column-major>`（不支持 `custom`）+ `--merge-decorate <none|reverse|snake|reverse-snake|snake-reverse>`
  决定列表铺进 `cols×rows` 画布的路径。

**退出码**：

| 码 | 含义                                        |
|---|-------------------------------------------|
| 0 | 成功                                        |
| 1 | 参数错误（缺必填 / 格式非法 / 互斥冲突，早于 Core 调用）        |
| 2 | 运行时错误（Core 业务错误，如坍缩不可行）                   |
| 3 | 输入文件错误（图像不存在 / 不可读 / 格式不支持）               |
| 4 | 输出失败（目录不可创建 / 文件被占用 / 磁盘满）                |
| 5 | 内部错误（未预期异常）                               |

**错误输出格式**（stderr）：

```text
idc: error: <简述>
  详情: <细节>
  提示: <可操作建议>
```

### 8.2 GUI 行为契约（桌面）

桌面 GUI 必须支持的操作能力清单（交互细节见各 GUI 文档）：

- **模式与极性**：L1/L2/L3 切换；极性 keep/remove 切换（L1 恒 keep）。
- **画布**：框选、选区边/角拖拽调整、贯穿切割线拖拽、网格线拖拽（L3）、缩放 / 平移 / 适应窗口、
  坐标吸附（图像边缘 / 网格线 / 中心线）、键盘微调。
- **预处理 8 项**：旋转 / 翻转 / 缩放 / 尺寸 / 黑白 / 反色 / 色道分离 / 重置；维度变化时自动清选区。
- **标注**：全工具绘制、选中移动、OBB 手柄（8 缩放 + 1 旋转）、属性编辑（颜色 / 粗细 / 填充 / 文字 / 字号 / 变换）、
  删除选中、清除全部（二次确认）。
- **导出**：分离 / 坍缩 / 重排三式 + 质量参数 + 烧录选项；不可坍缩时提示原因并降级重排（§4.4）。
- **配置存取**：保存 / 加载 JSON（§7 schema）。
- **撤销 / 重做双域**：文档态与标注态按上下文路由，菜单启用态跟随。
- **状态栏**：坐标 / 光标 RGB / 当前模式 / 保留块数 / 缩放 / 提示；错误非模态限时显示。

**快捷键语义**：`1/2/3` 模式切换、`K/R` 极性切换、`Delete/Backspace` 删除选中、`Esc` 取消 / 清选区 / 收笔、
方向键 1px 微调、`Ctrl+Z/Y` 撤销 / 重做。**焦点守卫**：焦点位于文本 / 数值输入框时，画布快捷键不得触发。

### 8.3 移动端 GUI 行为契约（Android / iOS）

- **行为同构**：移动端满足 §8.2 全部能力与 §1-7 基线，**不做任何行为分支**。
- **触控精度**：画布可抓取元素（切割线 / 选区边角 / 标注 / OBB 手柄）抓取半径 ≥ 12pt；
  可点控件 ≥ 44×44pt；相邻目标过密时提供辅助放大 / 吸附。
- **零键盘约束**：§8.2 的每条键盘 / 右键 / 悬停能力必须有触控等价物——
  模式与极性按钮、选中浮动工具条「删除」、「取消 / 完成」浮动按钮（替代 Esc）、8 向步进盘（替代方向键）、
  撤销 / 重做常驻按钮、长按（≥ 500ms）弹出右键等价菜单。
- **手势映射**：单击 = 轻点；拖拽 = 单指按住拖动；缩放 = 双指捏合；平移 = 工具未激活时单指 /
  激活时双指；适应窗口 = 双击画布；框选 = 工具未激活时单指拖出矩形。
- **方向**：竖屏与横屏均为合法形态；旋转只重排 UI，选区 / 标注 / 撤销栈 / Document 状态无损。
- **布局**：移动端为「画布为主 + 抽屉式面板」（桌面三栏折叠为底部工具条 / 抽屉）；
  状态栏精简为模式 / 保留块数 / 缩放，详情入抽屉。
- **导出落点**：Android 统一导出至系统相册公共目录 `Pictures/ImageDiscropper`（相册 / 文件管理器可见）；
  文件名支持面板自定义（留空自动命名，同名自动加时间戳后缀）；失败落应用文档目录并提示。
  移动端不提供文件 / 目录对话框（SAF 自定义目录受系统目录权限与提供者差异限制，不可靠）。
  桌面端保持对话框选路径不变。
- **验收**：§8.2 每个能力均可纯触屏完成；桌面构建与移动构建并存互不干扰。
