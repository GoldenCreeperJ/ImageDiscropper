# src/history

**目录作用**：`include/history` 下模板类的实现补充。

**分块依据**：`HistoryManager<T>` 是纯模板类，实现全部放在头文件中。
本目录仅保留一个 `.cpp` 用于显式实例化常用类型，避免每个使用方重复编译模板代码。

| 文件                    | 职责                  |
|-----------------------|---------------------|
| `history_manager.cpp` | 显式实例化说明与未来扩展的辅助函数占位 |
