# 给 AI Agent 的任务说明书
> **任务目标**：依据《图像区域提取、反向剔除与网格分割工具 · 需求规格说明（终稿 v1.0）》，快速生成一个可运行的Core，实现 MVP 范围内的完整功能。
>
> **使用方式**：本说明书是终稿的可执行化版本。Agent 应把本说明书当作**唯一的施工图**，终稿中未在此处明确的内容，以终稿为准；两者冲突时，以终稿为准。

## 🎯 项目核心哲学 (必须牢记)
常规裁剪只回答“保留哪一块”；本工具回答“**沿哪些线切开、留下哪些块、怎么重新拼起来**”。
所有功能必须基于统一的 **Grid-Selection-Emit 模型** 实现：
`原图 → ① 切割线集合 → ② 诱导网格 → ③ 选择集 → ④ 极性(Keep/Remove) → ⑤ 排布导出(Collapse/Rearrange)`。
**严禁**为 L1(标准提取)、L2(反向剔除)、L3(网格分割) 编写三套独立的裁剪逻辑，它们必须是同一引擎的不同参数预设。  
**公理不可违反**：切割线是贯穿全图的直线，不是线段。任何实现若把切割理解为“线段围成的框”，视为违反需求。

## ⚠️ 绝对工程规范 (Strict Rules - 违反即视为不合格)
1. **目录说明**：根目录必须有 `README.md` (含应用介绍、组成、终稿概念映射)。**每个子目录**必须包含 `README.md` 说明该目录作用。
2. **注释规范**：
    - **每个文件头部**必须包含注释
    - **每个函数首部**必须包含注释
    - **代码内部**：在复杂逻辑分块处，必须用中文注释注明。
3. **模块化拆分**：严禁“上帝文件”(God File)。代码必须按功能、任务严格分文件撰写。


---

## 一、任务边界

- 统一引擎（Grid-Selection-Emit 模型）
- L2 反向剔除模式：单矩形剔除、横线剔除、竖线剔除
- L1 标准提取模式：十字保留、横线保留、竖线保留
- L3 网格分割模式：网格定义、单元选择、极性、排序、分离导出、重排合并导出
- 极性开关（keep / remove 一键切换，实时预览）
- 导出：多图分离导出、坍缩式合并导出
- 配置保存与复用
- 多矩形并集剔除

---

## 二、功能实现清单


### 2.1 统一引擎（F-1）

**输入**：

- 原图（RGBA 或 RGB 像素缓冲）
- 切割线集合 `X`、`Y`
- 选择集 `S`（单元格索引集合）
- 极性 `polarity ∈ {keep, remove}`
- 输出配置（separate / merged-collapse / merged-rearrange + 参数）

**输出**：

- 保留块列表（每个块含：源区域、像素缓冲）
- 或合并后的单图

**核心流程**：

1. 由 `X`、`Y` 生成诱导网格 `C[i][j]`。
2. 应用极性得到保留集 `R`。
3. 根据输出模式：
   - `separate`：每个保留单元独立成图。
   - `merged-collapse`：先检查坍缩可行性（见 4.4），可行则紧贴拼接。
   - `merged-rearrange`：按序列填入新画布。

**实现约束**：

- 切割线去重、排序、裁剪到 `[0, W]` / `[0, H]`。
- 空块（宽或高为 0）必须跳过。
- 像素归属统一左闭右开 `[x1, x2)`。
- 所有几何计算使用整数像素。


### 2.4 坍缩可行性判定（F-6）

**定理**：保留集 `R` 能无空洞、无重叠地坍缩为矩形图，当且仅当被剔除的单元恰好构成诱导网格中的若干整行和/或整列。

**MVP 中满足坍缩条件的情况**：

- 单矩形十字切割（L2 十字剔除）
- 横线切割（L2 横带剔除）
- 竖线切割（L2 竖带剔除）

**不满足时**：坍缩式合并自动禁用，UI 提示原因，降级为重排式合并。

**实现提示**：坍缩式合并 ≡ 先删整列条带、再删整行条带，两次一维拼接的复合。不必真的切成九块。

### 2.5 边界与异常（必须处理）

| 编号  | 情况                      | 处理                                     |
|-----|-------------------------|----------------------------------------|
| E-1 | `x1 >= x2` 或 `y1 >= y2` | 禁止生成切割区域，提示用户。                         |
| E-2 | 切割线超出图像边界               | 自动裁剪到 `[0, W] × [0, H]`。               |
| E-3 | 退化选框 `x1 = x2`          | 不产生竖切割，降级为横线切割。                        |
| E-4 | 贴边选框 `x1 = 0`           | 左侧块宽度为 0，导出时跳过空块。                      |
| E-5 | 网格单元格部分超出图像             | 按余量策略处理（discard / keep-partial / pad）。 |
| E-6 | 删除区域为空或覆盖全图             | 提示用户，不执行导出。                            |
| E-7 | 保留区域为空                  | 禁止导出，提示用户。                             |
| E-8 | 导出路径不存在 / 文件被占用         | 给出明确错误提示。                              |


---

## 三. 验收清单

Agent 在交付前必须逐项确认：

### 3.1 功能验收

- [ ] L1 十字保留：输出中心矩形，尺寸正确。
- [ ] L1 横线保留：输出中间横带，尺寸正确。
- [ ] L1 竖线保留：输出中间竖带，尺寸正确。
- [ ] L2 十字切割：输出四角，位置正确（左上→(0,0)，右上→(x1,0)，左下→(0,y1)，右下→(x1,y1)）。
- [ ] L2 横线切割：输出上下两块，坍缩后尺寸为 `W × (H−Δy)`。
- [ ] L2 竖线切割：输出左右两块，坍缩后尺寸为 `(W−Δx) × H`。
- [ ] 极性开关：keep / remove 切换后预览即时更新。
- [ ] 分离导出：每块一图，命名符合模板，zip 打包。
- [ ] 坍缩合并：满足条件时输出正确尺寸。
- [ ] 坍缩不可行时：自动禁用并提示，可降级为重排。
- [ ] 重排合并：按序列填入指定画布，空位填充正确。
- [ ] L3 网格定义：基准点、单元尺寸、行列数、余量策略均生效。
- [ ] L3 单元选择：单击、框选、全选、反选、反相均生效。
- [ ] L3 排序：横优先、竖优先、逆向、蛇形、自定义均生效。
- [ ] L3 重排导出：按序填入画布，输出正确。

### 3.2 边界验收

- [ ] E-1 至 E-8 全部有对应测试并通过。
- [ ] 空块被跳过。
- [ ] 像素归属无重复无丢失。


## 四、重要提醒

- **终稿是唯一需求基线**。任何设计决策与终稿冲突时，以终稿为准。
- **不要提前引入 DLL / C ABI / Python binding**。Core 先作为普通 library，FFI 留空占位即可。
- **不要把所有功能塞进一个文件**。Core 按阶段分模块，前端按组件分文件。
- **不要省略注释**。文件头、函数头、关键算法块必须有中文说明。
- **不要绕过统一引擎**。L1/L2/L3 必须走同一条代码路径，模式只是参数预设。
- **优先保证可演示**。MVP 完整可跑通。

> 开始执行时，先输出一份执行计划（含文件清单与依赖顺序），确认后再逐文件生成。




