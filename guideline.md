# 给 AI Agent 的任务说明书
> **任务目标**：依据《图像区域提取、反向剔除与网格分割工具 · 需求规格说明（终稿 v1.0）》，快速生成一个可运行的CLI壳，实现 MVP 范围内的完整功能。
>
> **使用方式**：本说明书是终稿的可执行化版本。Agent 应把本说明书当作**唯一的施工图**，终稿中未在此处明确的内容，以终稿为准；两者冲突时，以终稿为准。

## 🎯 项目核心哲学 (必须牢记)
常规裁剪只回答“保留哪一块”；本工具回答“**沿哪些线切开、留下哪些块、怎么重新拼起来**”。
所有功能必须基于统一的 **Grid-Selection-Emit 模型** 实现：
`原图 → ① 切割线集合 → ② 诱导网格 → ③ 选择集 → ④ 极性(Keep/Remove) → ⑤ 排布导出(Collapse/Rearrange)`。
**严禁**为 L1(标准提取)、L2(反向剔除)、L3(网格分割) 编写三套独立的裁剪逻辑，它们必须是同一引擎的不同参数预设。  
**公理不可违反**：切割线是贯穿全图的直线，不是线段。任何实现若把切割理解为“线段围成的框”，视为违反需求。

## ⚠️ 绝对工程规范 (Strict Rules - 违反即视为不合格)
1. **目录说明**：根目录必须有 `README.md` (含应用介绍、组成、终稿概念映射)。**每个子目录**必须包含 `README.md` 说明该目录作用。
2. **注释规范**：
    - **每个文件头部**必须包含注释
    - **每个函数首部**必须包含注释
    - **代码内部**：在复杂逻辑分块处，必须用中文注释注明。
3. **模块化拆分**：严禁“上帝文件”(God File)。代码必须按功能、任务严格分文件撰写。

## 0. Agent 执行总则（必须遵守）

| 编号    | 规则                                                                                 |
|-------|------------------------------------------------------------------------------------|
| A-0.1 | **CLI 是薄壳**：CLI 只做四件事——解析参数、调用 Core、格式化输出、返回退出码。任何切割、几何、导出、排序、极性判断的逻辑都不得在 CLI 中实现。 |
| A-0.2 | **不重复造轮子**：Core 已提供 JSON、图像解码、几何计算、引擎流水线。CLI 必须直接调用，不得重新实现。                        |
| A-0.3 | **复用 Core 的依赖策略**：CLI 引入的第三方依赖必须与 Core 保持一致；若 Core 已 vendor 某库，CLI 直接复用，不新增版本。     |
| A-0.4 | **命令集对齐终稿**：CLI 命令必须覆盖终稿 §4.2 全部命令，且参数语义一致。不得自行增删命令或改变参数含义。                        |
| A-0.5 | **退出码规范**：0 成功，1 参数错误，2 运行时错误，3 输入文件错误，4 输出失败。详见 §5。                               |
| A-0.6 | **错误输出到 stderr**：所有错误信息必须写到 `stderr`，不得混入 `stdout`。`stdout` 只输出正常结果与进度信息。          |
| A-0.7 | **可验收**：必须满足 §10 验收清单的全部条目。                                                        |
| A-0.8 | **不引入网络依赖**：CLI 不得发起任何网络请求。                                                        |
| A-0.9 | **不实现 GUI 相关代码**：CLI 层不得包含任何窗口、渲染、事件循环相关代码。                                        |

---

## 1、任务边界

| 编号  | 交付物                 | 形式                             |
|-----|---------------------|--------------------------------|
| D-1 | 可运行的 CLI 二进制        | 支持 §4 全部命令                     |
| D-2 | CLI 目录 `README.md`  | 目录作用说明（见 §7.2）                 |
| D-3 | 每个源码目录的 `README.md` | 同上                             |
| D-4 | 每个源码文件的文件头注释        | 见 §7.3                         |
| D-5 | 每个函数的函数头注释          | 见 §7.4                         |
| D-6 | 代码分块依据注释            | 见 §7.5                         |
| D-7 | 集成测试                | 覆盖 L1 / L2 / L3 全部命令，见 §9      |
| D-9 | 示例脚本                | 覆盖 L1 / L2 / L3 各一个典型场景，见 §8.3 |

---

## 2、功能实现清单

CLI 是终稿所述工具的命令行入口。Agent 必须理解以下概念，才能在参数设计与帮助文本中准确表达。

**首先检查现有 Core，不要假设 Core API**：

1. 列出 Core 对外暴露的全部公开 API（头文件清单 + 函数签名）。
2. 列出 Core 的模式入口函数（extract / erase / grid 各自对应哪个函数）。
3. 列出 Core 的错误类型（`CoreError` 或等价类型）及其可区分的情形。
4. 列出 Core 的配置结构（对应终稿 §9 的 JSON 结构在 C++ 中的映射）。

若发现 Core 缺少 CLI 所需的某个接口（例如 CLI 需要读取当前导出块的尺寸），Agent **不得在 CLI 中补实现**，而应：

- 直接报告错误
- 在 CLI 代码中以 `// TODO(core): 需要 Core 提供 xxx` 标注。


### 2.1 三层模式

| 层级 | 模式     | CLI 命令    |
|----|--------|-----------|
| L1 | 标准提取模式 | `extract` |
| L2 | 反向剔除模式 | `erase`   |
| L3 | 网格分割模式 | `grid`    |

### 2.2 统一引擎

```text
原图 → ① 切割线集合 → ② 诱导网格 → ③ 选择集 → ④ 极性 → ⑤ 排布导出
```

CLI 的参数就是这五个阶段的参数化入口。

### 2.3 关键术语

CLI 的 `--help` 文本、错误提示、`README.md` 中使用的术语必须与终稿 §3.2 一致：

- 切割线（cut line）：贯穿全图的水平/垂直直线。
- 带（band）：相邻两条同向切割线之间的整条区域。
- 单元格（cell）：一条竖带与一条横带的交集。
- 十字带：矩形选区诱导的"竖带 ∪ 横带"。
- 极性（polarity）：`keep` / `remove`。
- 坍缩（collapse）：删除整行/整列后剩余单元紧贴拼接。
- 重排（rearrange）：按显式序列填入新画布。

### 2.4 CLI是否放入Core或是与其并列由你决定

---


## 4. 命令规格（必须实现，image-tool需替换成实际的命令名称，若有不恰当的参数名或参数顺序，以终稿为准进行修改）

命令语法采用以下约定：

- 子命令：`image-tool <command> [options]`
- 坐标参数格式：`x1,y1,x2,y2`（左闭右开，单位像素）
- 带参数格式：`a,b`（a 为起始坐标，b 为结束坐标）
- 网格参数格式：`x0,y0,cw,ch`
- 选择参数：`--keep r,c` 或 `--remove r,c`，可重复出现

### 4.1 全局选项

| 选项                 | 说明                           |
|--------------------|------------------------------|
| `--help` / `-h`    | 显示帮助信息。                      |
| `--version` / `-v` | 显示版本号。                       |
| `--verbose`        | 输出详细日志到 stderr。              |
| `--quiet`          | 静默模式，只输出错误。                  |
| `--config <file>`  | 从 JSON 配置文件读取参数（对应终稿 §9 结构）。 |

### 4.2 命令清单

#### 4.2.1 `extract` —— L1 标准提取模式

```bash
# 矩形保留（十字保留的中心单元）
image-tool extract --input photo.jpg --rect 100,100,300,250 --output out.png

# 横线保留
image-tool extract --input photo.jpg --hband 100,250 --output out.png

# 竖线保留
image-tool extract --input photo.jpg --vband 100,300 --output out.png
```

| 选项                   | 必填  | 说明                         |
|----------------------|-----|----------------------------|
| `--input <file>`     | 是   | 输入图像路径。                    |
| `--rect x1,y1,x2,y2` | 三选一 | 矩形区域。                      |
| `--hband y1,y2`      | 三选一 | 水平带。                       |
| `--vband x1,x2`      | 三选一 | 垂直带。                       |
| `--output <file>`    | 是   | 输出图像路径。                    |
| `--format <fmt>`     | 否   | 输出格式，默认由 `--output` 扩展名推断。 |

#### 4.2.2 `erase` —— L2 反向剔除模式

**支持四种模式：十字切割、横线切割、竖线切割、多矩形并集剔除。**

##### 4.2.2.1 十字切割 / 横线切割 / 竖线切割

```bash
# 十字切割：导出四角为多张图
image-tool erase --input photo.jpg --rect 100,100,300,250 \
  --output-dir ./out/ --format png

# 十字切割：坍缩合并为单图
image-tool erase --input photo.jpg --rect 100,100,300,250 \
  --merge collapse --output out.png

# 横线切割
image-tool erase --input photo.jpg --hband 100,250 \
  --merge collapse --output out.png

# 竖线切割
image-tool erase --input photo.jpg --vband 100,300 \
  --merge collapse --output out.png
```

##### 4.2.2.2 多矩形并集剔除（FR-L2.4）

**语义**：`--rect` 可重复出现多次。每次出现定义一个矩形，该矩形诱导一组十字带（贯穿全图的竖带 ∪ 横带）。删除区域是所有十字带的并集。

```bash
# 两个矩形并集剔除：删除各自十字带的并集
image-tool erase --input photo.jpg \
  --rect 100,100,200,150 \
  --rect 400,300,500,400 \
  --merge collapse --output out.png

# 三个矩形并集剔除，分离导出
image-tool erase --input photo.jpg \
  --rect 100,100,200,150 \
  --rect 400,300,500,400 \
  --rect 700,500,800,600 \
  --output-dir ./out/ --format png
```

**删除区域的形式化定义**：

```text
D = ⋃ᵢ ( VBand(x1ᵢ, x2ᵢ) ∪ HBand(y1ᵢ, y2ᵢ) )
Out = I \ D
```

**诱导网格**：由所有矩形的 `x1ᵢ, x2ᵢ` 决定竖线集合，由所有 `y1ᵢ, y2ᵢ` 决定横线集合，加上图像边界 `0, W, H`。

**单元格删除判定**：

- 设 `C_del` 为被任一矩形的竖带覆盖的列索引集合。
- 设 `R_del` 为被任一矩形的横带覆盖的行索引集合。
- 单元格 `(r, c)` 被删除 ⟺ `r ∈ R_del` 或 `c ∈ C_del`。
- 保留集 = `(所有行 \ R_del) × (所有列 \ C_del)`。

**坍缩**：由于删除集是若干完整行与完整列的并集，多矩形并集剔除**始终满足坍缩条件**。

##### 4.2.2.3 通用选项

| 选项                   | 必填                          | 说明                                       |
|----------------------|-----------------------------|------------------------------------------|
| `--input <file>`     | 是                           | 输入图像路径。                                  |
| `--rect x1,y1,x2,y2` | 与 `--hband` / `--vband` 三选一 | **可重复出现**。一次出现 = 单矩形十字切割；多次出现 = 多矩形并集剔除。 |
| `--hband y1,y2`      | 三选一                         | 在 y1、y2 处各有一条横线；删除中间横带。                  |
| `--vband x1,x2`      | 三选一                         | 在 x1、x2 处各有一条竖线；删除中间竖带。                  |
| `--merge <mode>`     | 否                           | `collapse` 或 `rearrange`。缺省为分离导出。        |
| `--output <file>`    | 合并时必填                       | 合并输出路径。                                  |
| `--output-dir <dir>` | 分离时必填                       | 分离输出目录。                                  |
| `--format <fmt>`     | 否                           | 输出格式。                                    |
| `--naming <tpl>`     | 否                           | 命名模板，默认 `{name}_{index:03d}`。            |

**约束**：

- `--rect`、`--hband`、`--vband` 三者互斥，不得混用。
- `--rect` 可出现 1 次或多次；出现多次时语义为多矩形并集剔除。
- `--merge` 与 `--output-dir` 不得同时出现；分离导出必须指定 `--output-dir`。
- 违反以上任意一条，退出码 1。

**坍缩不可行时**：多矩形并集剔除不触发此情况。其他三种模式按 §4.2.2.4 处理。

##### 4.2.2.4 坍缩不可行时的行为（仅适用于十字 / 横线 / 竖线切割）

- 用户显式 `--merge collapse`：报错，提示改用 `--merge rearrange`（退出码 2）。
- 用户未指定 `--merge`：默认分离导出。


#### 4.2.3 `grid` —— L3 网格分割模式

```bash
# 3x3 网格，保留四角，按横优先排序合成单图
image-tool grid --input photo.jpg --grid 100,100,200,150 \
  --keep 0,0 --keep 0,2 --keep 2,0 --keep 2,2 \
  --sort row-major --compose --canvas 8x6 --output result.png
```

| 选项                    | 必填               | 说明                                                                                     |
|-----------------------|------------------|----------------------------------------------------------------------------------------|
| `--input <file>`      | 是                | 输入图像路径。                                                                                |
| `--grid x0,y0,cw,ch`  | 是                | 网格定义。                                                                                  |
| `--keep r,c`          | 与 `--remove` 二选一 | 保留指定单元格，可重复。                                                                           |
| `--remove r,c`        | 与 `--keep` 二选一   | 删除指定单元格，可重复。                                                                           |
| `--sort <strategy>`   | 否                | `row-major` / `column-major` / `custom`。默认 `row-major`。                                |
| `--decorate <method>` | 否                | `reverse` / `snake` /`reverse-snake`/`snake-reverse` / `none`。默认 `none`。在`custom`下不可用。 |   
| `--order r,c;r,c;...` | 否                | 自定义序列，`--sort custom` 时必填。                                                             |
| `--margin <policy>`   | 否                | 余量策略：`discard` / `keep-partial` / `pad`。默认 `discard`。                                  |
| `--pad-color <hex>`   | 否                | 填充色，默认透明。                                                                              |
| `--compose`           | 否                | 合并为单图；缺省为分离导出。                                                                         |
| `--canvas ColxRow`    | `--compose` 时必填  | 输出画布尺寸，如 `8x6`。                                                                        |
| `--output <file>`     | `--compose` 时必填  | 输出路径。                                                                                  |
| `--output-dir <dir>`  | 分离时必填            | 输出目录。                                                                                  |
| `--format <fmt>`      | 否                | 输出格式。                                                                                  |
| `--naming <tpl>`      | 否                | 命名模板。                                                                                  |

**重要约束**：`--keep` 与 `--remove` 不得同时出现，否则报参数错误（退出码 1）。

#### 4.2.4 `config` —— 配置文件操作（推荐实现）

```bash
# 从配置文件执行
image-tool config --load my-config.json --output result.png

# 将当前参数保存为配置文件（dry-run 模式）
image-tool extract --input photo.jpg --rect 100,100,300,250 \
  --output out.png --save-config out.json
```

| 选项                     | 说明                   |
|------------------------|----------------------|
| `--load <file>`        | 从 JSON 配置文件读取全部参数。   |
| `--save-config <file>` | 将当前参数序列化为 JSON 配置文件。 |

配置文件结构必须与终稿 §9 一致。CLI 必须复用 Core 的 JSON 模块，不得自行实现解析。

---

## 5. 退出码与错误处理

### 5.1 退出码规范

| 退出码 | 含义     | 典型场景                        |
|-----|--------|-----------------------------|
| 0   | 成功     | 命令正常完成。                     |
| 1   | 参数错误   | 缺少必填参数、参数格式错误、互斥参数同时出现。     |
| 2   | 运行时错误  | Core 返回的业务错误，如坍缩不可行、保留区域为空。 |
| 3   | 输入文件错误 | 输入文件不存在、无法读取、格式不支持。         |
| 4   | 输出失败   | 输出目录不存在、文件被占用、磁盘满。          |
| 5   | 内部错误   | 未预期的异常，需上报 bug。             |

### 5.2 错误信息格式

错误信息必须写到 `stderr`，格式：

```text
image-tool: error: <简短描述>
  详情: <Core 返回的错误详情>
  提示: <可操作的建议>
```

示例：

```text
image-tool: error: 当前选择集不满足坍缩条件
  详情: Core 返回：保留集不构成整行或整列的补集
  提示: 请改用 --merge rearrange，或调整切割线使删除区域覆盖整行/整列
```

### 5.3 参数校验顺序

CLI 必须按以下顺序校验：

1. 命令是否存在。
2. 全局选项是否合法。
3. 必填参数是否齐全。
4. 互斥参数是否冲突。
5. 参数格式是否合法（坐标、网格、模板等）。
6. 输入文件是否可读。
7. 调用 Core 执行。

前 5 步的错误必须在调用 Core 之前返回，退出码为 1。

---

## 6. 帮助文本与版本信息

### 6.1 `--help` 输出要求

`--help` 必须包含：

- 工具名称与一句话定位。
- 三层模式说明（L1 / L2 / L3）。
- 全部子命令列表及一句话说明。
- 全局选项。
- 常用示例（至少 L1 / L2 / L3 各一个）。

子命令的 `--help`（如 `image-tool erase --help`）必须包含：

- 命令的语义说明（对应终稿中的哪一层、哪个模式）。
- 全部选项及其含义、默认值、必填性。
- 该命令的完整示例。
- 常见错误提示。

### 6.2 `--version` 输出要求

格式：

```text
image-tool <version> (core <core-version>)
```

版本号必须与构建系统保持一致。

---

### 8.1 示例脚本

`examples/` 目录下必须提供：

| 脚本                     | 覆盖场景                 |
|------------------------|----------------------|
| `l1_extract.sh`        | L1 矩形保留              |
| `l2_erase_collapse.sh` | L2 十字切割 + 坍缩合并       |
| `l2_erase_separate.sh` | L2 十字切割 + 分离导出到文件夹 |
| `l3_grid_compose.sh`   | L3 网格选择 + 重排合并       |

每个脚本必须：

- 使用仓库内的示例图片。
- 输出到临时目录，不污染仓库。
- 带注释说明每一步的作用。

---

## 9. 测试要求

### 9.1 单元测试

- 参数解析：`--rect`、`--hband`、`--vband`、`--grid`、`--keep`、`--remove`、`--sort`、`--order` 等全部选项的解析必须覆盖。
- 命名模板解析：`{name}_{index:03d}`、`{name}_r{row}c{col}` 等。
- 退出码映射：每种 Core 错误类型映射到正确的退出码。

### 9.2 集成测试

必须覆盖以下场景：

| 编号    | 场景                               | 验证点                      |
|-------|----------------------------------|--------------------------|
| IT-1  | `extract --rect`                 | 输出尺寸正确。                  |
| IT-2  | `extract --hband`                | 输出尺寸正确。                  |
| IT-3  | `extract --vband`                | 输出尺寸正确。                  |
| IT-4  | `erase --rect --output-dir`      | 输出 4 个文件，位置正确。           |
| IT-5  | `erase --rect --merge collapse`  | 输出尺寸为 `(W−Δx) × (H−Δy)`。 |
| IT-6  | `erase --hband --merge collapse` | 输出尺寸正确。                  |
| IT-7  | `erase --vband --merge collapse` | 输出尺寸正确。                  |
| IT-8  | `erase` 多矩形并集剔除             | 删除各十字带并集，坍缩尺寸正确。             |
| IT-9  | `grid --keep` + `--compose`      | 输出画布尺寸正确，单元格位置正确。        |
| IT-10 | `grid --sort column-major`       | 排序结果与预期一致。               |
| IT-11 | `grid --sort custom --order`     | 自定义序列生效。                 |
| IT-12 | `config --load`                  | 配置文件参数全部生效。              |
| IT-13 | `--version`                      | 输出格式正确。                  |
| IT-14 | `--help`                         | 输出包含全部子命令。               |
| IT-15 | 缺少必填参数                           | 退出码为 1，stderr 有提示。       |
| IT-16 | 输入文件不存在                          | 退出码为 3。                  |
| IT-17 | 输出目录不存在                          | 退出码为 4。                  |
| IT-18 | 坍缩不可行                            | 退出码为 2，提示改用 rearrange。   |

### 9.3 测试运行方式

- 测试必须可通过一条命令运行（如 `ctest` 或等价）。
- 测试必须不依赖外部网络。
- 测试必须不污染仓库目录。

---

## 10. 验收清单

Agent 在交付前必须逐项确认：

### 10.1 命令验收

- [ ] `extract` 支持 `--rect` / `--hband` / `--vband`，输出正确。
- [ ] `erase` 支持分离导出、坍缩合并、重排合并。
- [ ] `grid` 支持网格定义、选择集、排序、余量策略、合并导出。
- [ ] `config --load` / `--save-config` 生效。
- [ ] `--help` / `--version` 输出符合 §6。

### 10.2 错误处理验收

- [ ] 退出码符合 §5.1。
- [ ] 错误信息格式符合 §5.2。
- [ ] 参数校验顺序符合 §5.3。
- [ ] 坍缩不可行时行为符合 §4.2.2。

### 10.3 工程验收

- [ ] 每个源码目录有 `README.md`。
- [ ] 每个文件有文件头注释。
- [ ] 每个函数有函数头注释。
- [ ] 代码分块依据以注释形式注明。
- [ ] 中文注释完整。
- [ ] 目录结构符合 §8。
- [ ] 单元测试覆盖参数解析全部选项。
- [ ] 集成测试覆盖 IT-1 至 IT-18。

### 10.4 示例验收

- [ ] `examples/` 下四个脚本均可运行且输出正确。
- [ ] 脚本带中文注释。
- [ ] 脚本不污染仓库。

---

## 11. Agent 交付格式

Agent 完成开发后，必须输出：

1. **完成报告**：逐项对照 §10 验收清单，标注通过 / 未通过。
2. **Core 接口确认清单**：实际使用的 Core API 列表。
3. **目录树**：实际生成的完整目录树。
4. **运行演示**：§4.2 中全部命令的实际输出（含 `--help` 与错误示例）。
5. **已知限制**：明确列出未实现的功能与原因。
6. **待 Core 补充接口**：若发现 Core 缺少 CLI 所需接口，列出清单。

---


## 四、重要提醒

- **终稿是唯一需求基线**。任何设计决策与终稿冲突时，以终稿为准。
- **不要把所有功能塞进一个文件**。
- **不要省略注释**。文件头、函数头、关键算法块必须有中文说明。
- **不要绕过统一引擎**。L1/L2/L3 必须走同一条代码路径，模式只是参数预设。
- **优先保证可演示**。MVP 完整可跑通。

> 开始执行时，先输出一份执行计划（含文件清单与依赖顺序），确认后再逐文件生成。



