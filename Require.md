# 新项目要求
# 图像区域提取、反向剔除与网格分割工具 · 需求规格说明（终稿 v1.0）

---

## 1. 文档摘要

> **一句话定位**：常规裁剪只回答“保留哪一块”；本工具回答“**沿哪些线切开、留下哪些块、怎么重新拼起来**”。

本工具基于**轴对齐切割线**与**规则网格**，以“规则化区域分割、布尔区域运算、网格化离散与重组”为核心，将图像处理能力划分为三层：

| 层级     | 模式         | 定位          | 目标用户      | 交互成本            |
|--------|------------|-------------|-----------|-----------------|
| **L1** | 标准提取模式     | 兼容基线        | 所有人       | 1 次拖拽           |
| **L2** | **反向剔除模式** | **⭐ 差异化内核** | 有明确痛点的用户  | 1 次拖拽 + 1 次极性切换 |
| **L3** | **网格分割模式** | **⭐ 完备表达层** | 进阶 / 批量作业 | 参数配置 + 多次点选     |

**核心判断**：三者在数学上是包含关系（L1 ⊂ L2 ⊂ L3），但在产品上必须并列呈现。L1 是快捷入口，L2 是核心特色，L3 是高级编排器。

---

## 2. 统一引擎：Grid-Selection-Emit 模型

所有功能均归约为以下流水线：

```text
原图 → ① 切割线集合 → ② 诱导网格 → ③ 选择集 → ④ 极性 → ⑤ 排布导出
```

| 阶段     | 定义                                                                                                   |
|--------|------------------------------------------------------------------------------------------------------|
| ① 切割线  | 竖线 `X = {0 = x₀ < x₁ < … < xₘ = W}`，横线 `Y = {0 = y₀ < y₁ < … < yₙ = H}`。**贯穿全图的直线，不是线段**——这是整个工具的公理。 |
| ② 诱导网格 | `m × n` 个单元 `C[i][j] = [xᵢ, xᵢ₊₁) × [yⱼ, yⱼ₊₁)`，互不重叠且恰好铺满原图。                                         |
| ③ 选择集  | `S ⊆ {C[i][j]}`。                                                                                     |
| ④ 极性   | `keep` → 保留集 `R = S`；`remove` → `R = 全集 \ S`。                                                        |
| ⑤ 输出   | `separate`（多图分离）或 `merged`（合并单图）；合并又分 `collapse`（坍缩）与 `rearrange`（重排）。                               |

**所有已列功能都是该模型的特例。** 不要把“十字切割”“竖线保留”“网格删除”当成 8 个独立功能去实现，它们是 `(切割线生成方式, 极性, 输出方式)` 的组合。

> **工程要求 NFR-0：先写引擎，再写 UI。** 三种模式必须走同一条代码路径。若按模式分头实现 UI 逻辑，后期无法收敛。

---

## 3. 术语与坐标约定

### 3.1 坐标约定

- 整数像素；区间左闭右开 `[a, b)`。
- 原点左上，`x` 向右，`y` 向下。
- 原图尺寸 `W × H`。
- 交互浮点坐标进入引擎前统一 `round`。

### 3.2 术语表

| 术语                  | 定义                                                      |
|---------------------|---------------------------------------------------------|
| **切割线（cut line）**   | 贯穿整幅图像的水平或垂直直线，而非线段。这是本工具区别于常规裁剪的根本前提。                  |
| **带（band）**         | 相邻两条同向切割线之间的整条区域；竖带全高，横带全宽。                             |
| **单元格（cell）**       | 一条竖带与一条横带的交集，是选择与导出的最小单位。                               |
| **十字带（cross band）** | 由一个矩形选区诱导的“竖带 ∪ 横带”。                                    |
| **极性（polarity）**    | `keep` / `remove`，决定选择集是保留还是剔除。                         |
| **坍缩（collapse）**    | 删除若干整行/整列单元后，将剩余单元向原点方向紧贴拼接。                            |
| **重排（rearrange）**   | 按显式序列将保留块填入新画布，允许改变相对位置。                                |
| **选区锚点**            | 用户框选产生的矩形对角坐标 `(x1, y1, x2, y2)`，满足 `x1 < x2, y1 < y2`。 |
| **区域片段（Fragment）**  | 经切割后产生的每一个独立矩形像素块。                                      |

### 3.3 基本形状

由两条水平线和两条竖直线定义：

| 名称   | 定义                                                      |
|------|---------------------------------------------------------|
| 中心矩形 | `Rect(x1, y1, x2, y2)`                                  |
| 水平带  | `HBand(y1, y2) = Rect(0, y1, W, y2)`                    |
| 垂直带  | `VBand(x1, x2) = Rect(x1, 0, x2, H)`                    |
| 十字区域 | `Cross(x1, x2, y1, y2) = HBand(y1, y2) ∪ VBand(x1, x2)` |
| 四角区域 | `I \ Cross(x1, x2, y1, y2)`                             |

---

## 4. 功能需求

### 4.1 基础图像处理（FR-1，前置层）

作用于原图，在切割之前执行，产出图像成为切割引擎输入。

| ID     | 功能 | 要求                                                                                            |
|--------|----|-----------------------------------------------------------------------------------------------|
| FR-1.1 | 旋转 | 90° 步进（无损，仅重排像素）+ 任意角度（需指定插值方式与背景填充）。                                                         |
| FR-1.2 | 翻转 | 水平 / 垂直。                                                                                      |
| FR-1.3 | 标注 | 矩形/正方形/菱形、圆形/椭圆/扇形、三角/等腰/等边/直角、直线/箭头/多线段、文字、画笔，自定义颜色、粗细、填充等；作为**独立图层**存在，可在导出时选择是否烧录（burn-in） |
| FR-1.4 | 调整 | 缩放、图像尺寸调整、黑白、反色、色道分离、马赛克、颜色选取。                                                                |
| FR-1.5 | 控制 | 撤销、重做、清除、预览。                                                                                  |

> **设计约束**：标注图层与切割操作解耦——切割时标注随像素一起被切开，而不是被单独处理。

---

### 4.2 L1 · 标准提取模式（兼容基线）

**定位**：用户已有的肌肉记忆，必须无缝支持，但不是产品价值所在。作用是降低首次使用门槛，并自然引导用户发现“反向”开关。

| 形态   | 切割线                              | 选择集  | 结果     |
|------|----------------------------------|------|--------|
| 十字保留 | `X={0,x1,x2,W}`, `Y={0,y1,y2,H}` | 中心单元 | 常规矩形裁剪 |
| 横线保留 | `X={0,W}`, `Y={0,y1,y2,H}`       | 中间横带 | 保留一条横条 |
| 竖线保留 | `X={0,x1,x2,W}`, `Y={0,H}`       | 中间竖带 | 保留一条竖条 |

极性恒为 `keep`，输出恒为单图，无需暴露额外选项。

---

### 4.3 L2 · 反向剔除模式 ⭐ 差异化内核

#### 4.3.1 独特性从何而来

**独特的不是“删除”，而是“按线删除”。** 这一点必须在需求文档和产品文案中反复强调，否则会被误认为 Photoshop 的“选区 + Delete”。

| 维度     | 常规工具的“删除”              | 本工具的反向剔除                |
|--------|------------------------|-------------------------|
| 切割对象   | 线段围成的**框**             | 贯穿全图的**直线**             |
| 结果     | 原图上留下一个洞               | 图像被真正**切成若干块**          |
| 尺寸     | 不变                     | **变小**（`W−Δx` × `H−Δy`） |
| 语义     | 遮盖 / 涂抹 / 修复           | **抽掉中缝，两侧对接**           |
| 是否需补像素 | 需要（填色或 AI 修复，必然引入伪造内容） | **完全不需要，零伪造、无损**        |

这是 crop 的对偶运算——同一个选框、同一组切割线，只是极性相反。数学上和裁剪一样基础、干净，但主流工具尚未实现。

#### 4.3.2 典型场景

1. **长截图删中段**：聊天记录/网页截图删掉中间无关的一段，上下自然衔接。
2. **表格删行列**：截图表格删掉某几列，左右对齐关系完全保持。
3. **删水印带 / 页眉页脚**：抽掉整条带而非涂黑。
4. **四角素材提取**：一次操作得到四个角块（十字切割），传统做法要裁 4 次。

#### 4.3.3 为什么不能只做 L3 网格模式代替它

- **交互成本差一个数量级**：反向剔除 = 1 次拖拽；网格实现同样效果 = 配置基准点与单元尺寸 + 点选 5 个单元。前者 O(1)，后者 O(n)。
- **心智模型完全不同**：用户想的是“把这一段去掉”，而不是“构造 3×3 网格再取补集”。
- **它是产品的传播点**：一句“能删掉中间那段”就能说清楚；“参数化网格分割引擎”说不清楚。

> **结论**：L2 在数学上是 L3 的子集，在产品上是**独立的一等公民**。必须给它专属入口、专属预览效果、专属文案。

#### 4.3.4 功能定义

| ID      | 需求                                               |
|---------|--------------------------------------------------|
| FR-L2.1 | **十字切割**：矩形选框 + `remove` → 保留左上、右上、左下、右下四块。      |
| FR-L2.2 | **横线切割**：两条横线 + `remove` → 保留上、下两块。              |
| FR-L2.3 | **竖线切割**：两条竖线 + `remove` → 保留左、右两块。              |
| FR-L2.4 | **多矩形并集剔除**：`k` 个矩形，删除区域取并集（语义见 §4.3.6）。         |
| FR-L2.5 | **极性开关**：与 L1 共用同一界面，一键在“保留框内 / 删除框内”之间切换，并实时预览。 |
| FR-L2.6 | 输出：多图分离导出 / 合并单图导出（坍缩式，见 §5.2）。                  |

单矩形十字切割的保留块（`0 ≤ x1 < x2 ≤ W`，`0 ≤ y1 < y2 ≤ H`）：

| 块  | 区域 `(左, 上, 右, 下)` | 尺寸                |
|----|-------------------|-------------------|
| 左上 | `(0, 0, x1, y1)`  | `x1 × y1`         |
| 右上 | `(x2, 0, W, y1)`  | `(W−x2) × y1`     |
| 左下 | `(0, y2, x1, H)`  | `x1 × (H−y2)`     |
| 右下 | `(x2, y2, W, H)`  | `(W−x2) × (H−y2)` |

#### 4.3.5 边界情况

1. **退化选框**：`x1 = x2` 时不产生竖切割，自动降级为横线切割（UI 须允许只画一条线）。
2. **贴边选框**：`x1 = 0` 时左侧两块宽度为 0，导出时跳过空块。
3. **像素归属**：统一 `[x1, x2)`，杜绝边界像素重复或丢失。

#### 4.3.6 多矩形语义

**方案 A：十字带并集**

删除区域：

```text
D = ⋃ᵢ ( VBand(x1ᵢ, x2ᵢ) ∪ HBand(y1ᵢ, y2ᵢ) )
```

输出：

```text
Out = I \ D
```

- 与单矩形逻辑严格一致，保持“按线切割”公理。
- 多个矩形可重叠、相邻或分离。
- 删除后保留区域可能不连通，系统应识别连通域，支持按连通域拆分导出或合并导出。
- 该方案下被删除的是若干整列和整行的并集，满足坍缩条件（见 §5.4）。

> 其他多矩形方案（如仅删除矩形内部）与“按线切割”公理冲突，舍弃。

---

### 4.4 L3 · 网格分割模式 ⭐ 完备表达层

#### 4.4.1 高级性从何而来

L3 不是“多切几刀”，而是从固定形态跃迁到任意形态。三个质变：

| 维度    | L1 / L2    | L3                      |
|-------|------------|-------------------------|
| 切割线数量 | 固定 2 条/轴   | 任意条，参数化生成               |
| 选择集形状 | 由形态决定      | 任意子集，逐单元自由点选            |
| 单元顺序  | 隐含空间顺序     | **可编排的显式序列**            |
| 输出布局  | 只能坍缩回原相对位置 | 可重排为任意 `cols × rows` 画布 |

**“顺序”是 L3 独有的第三个自由度。** L1/L2 的输出块只有空间关系，L3 引入序号，于是“切割”变成“切割 + 重组”——可做拼图打乱、条漫切分、精灵图拆解与重打包。

#### 4.4.2 功能定义

| ID      | 需求                                                                                                            |
|---------|---------------------------------------------------------------------------------------------------------------|
| FR-L3.1 | **网格定义**：基准点 `(x₀, y₀)`、单元尺寸 `(cw, ch)`、。                                                                     |
| FR-L3.2 | **余量策略**：`W − x₀` 非 `cw` 整数倍时须显式选择——`discard` 丢弃 / `keep-partial` 保留残缺单元 / `pad` 补白至完整单元。                     |
| FR-L3.3 | **单元选择**：单击切换、拖拽框选、全选 / 反选 / 反相。                                                                              |
| FR-L3.4 | **极性**：`keep` 保留选中 / `remove` 剔除选中，与 L1/L2 同一套开关。                                                             |
| FR-L3.5 | **排序策略**：`row-major`（横优先）、`column-major`（竖优先），各自可叠加 `reverse`、`snake`；以及 `custom`（点选顺序即序号，单元角标实时显示编号，支持拖拽调序）。 |
| FR-L3.6 | **分离导出**：按序命名，打包 zip。                                                                                         |
| FR-L3.7 | **重排合并导出**：按序填入用户指定的 `cols × rows` 画布，自定义单元长宽，空位与余量以指定颜色（默认透明）填充。                                             |
| FR-L3.8 | **配置保存与复用**：网格参数可存为预设，应用到其他同尺寸图像（批处理基础）。                                                                      |

#### 4.4.3 与 L2 的关系

- L3 能表达 L1/L2 的一切结果，但不替代它们。
- 产品上的正确说法：L1/L2 是**高频操作的快捷路径**，L3 是**兜底的完备能力**。
- UI 建议提供「**转为网格模式编辑**」入口：用户在 L2 画完选框后，可一键把当前 3×3 诱导网格送进 L3 继续精修。

#### 4.4.4 网格定义
网格只允许通过以下几何参数定义：

- 基准点 x0
- 基准点 y0
- 单元宽度 cellWidth
- 单元高度 cellHeight

其中：

- x0、y0 决定网格切割线的起始位置；
- cellWidth 决定垂直切割线之间的周期距离；
- cellHeight 决定水平切割线之间的周期距离；
- 所有切割线均为贯穿整幅图像的完整水平线 / 垂直线；
- 行数、列数由图像边界与上述参数自动推导，不作为用户必须指定的独立参数；
- 单元格坐标和尺寸均由切割线与图像边界共同计算得到；
- 不存在仅在局部网格区域内生效的切割线。

因此，L3 网格的本质不是“指定一个 rows × cols 的网格”，而是：

> **以基准点为参考、以单元尺寸为周期生成贯穿整幅图像的水平/垂直切割线，再由这些切割线诱导出网格单元。**

---

## 5. 导出规格（三层共用）

### 5.1 分离导出

每个保留单元一张图，命名模板可配置（如 `{name}_{index:03d}` / `{name}_r{row}c{col}`），多文件打包 zip。

### 5.2 合并导出 · 坍缩式（L1/L2 主用）

剩余块按原相对位置紧贴拼接，结果尺寸 `(W − Δx) × (H − Δy)`。十字切割的贴图位置：

- 左上 → `(0, 0)`
- 右上 → `(x1, 0)`
- 左下 → `(0, y1)`
- 右下 → `(x1, y1)`

> **实现提示**：不必真的切成九块。坍缩式合并 ≡ 先删整列条带、再删整行条带，两次一维拼接的复合。代码更短、更不易错。

### 5.3 合并导出 · 重排式（L3 主用）

按 FR-L3.5 的序列填入指定画布，空位以填充色补齐。

### 5.4 关键约束：坍缩的可行性条件

> **定理**：保留集 `R` 能无空洞、无重叠地坍缩为矩形图，当且仅当被剔除的单元恰好构成诱导网格中的**若干整行和/或整列**。

- 单矩形十字切割 ✅ 满足
- 横线 / 竖线切割 ✅ 满足
- 多矩形并集剔除✅ 满足
- L3 任意选择集 ❌ 一般不满足

**处理策略**：不满足时，坍缩式合并自动禁用并提示原因，降级为重排式合并（空缺填充色补齐）。**不引入装箱算法**——复杂度与收益不成正比，明确列为 out of scope。

### 5.5 格式

PNG（支持透明）、JPEG（须指定背景色替代透明）、WebP、BMP 等；可选质量参数。

---

## 6. 边界与异常

- `x1 >= x2` 或 `y1 >= y2` 时禁止生成切割区域。
- 切割线超出图像边界时，自动裁剪到 `[0, W] × [0, H]`。
- 网格单元格部分超出图像时，可选择：裁剪到图像内 / 留空 / 填充背景色。
- 多矩形切割时，删除区域为空或覆盖全图时给出提示。
- 保留区域为空时禁止导出或提示用户。
- 导出路径不存在、文件被占用时给出错误提示。

---

## 7. 非功能需求

| ID    | 需求                                                         |
|-------|------------------------------------------------------------|
| NFR-0 | **引擎优先**：三层模式共用同一核心，模式仅为参数预设。                              |
| NFR-1 | **本地处理**：图像不上传服务器。                                         |
| NFR-2 | **无损优先**：切割为纯像素搬运，不重编码；仅 JPEG 输出允许有损。                      |
| NFR-3 | **性能**：8000×8000 px 图像预览交互 ≥ 30 fps（预览用降采样副本，导出时才对原分辨率操作）。 |
| NFR-4 | **可逆性**：全流程撤销 / 重做；参数序列化为配置对象，可导出复用。                       |
| NFR-5 | **精确输入**：所有几何参数除拖拽外均支持数值直接输入。                              |
| NFR-6 | **实时预览**：极性切换、排序变更须即时反映在画布上——这是让用户“看懂”反向剔除的关键。             |
| NFR-7 | **吸附与微调**：支持鼠标拖拽切割线、网格线；支持键盘微调坐标；支持坐标吸附：图像边缘、网格线、中心线。      |
| NFR-8 | **大图处理**：支持大图分块处理，避免内存溢出。                                  |
| NFR-9 | **元数据**：导出时保留元数据可选。                                        |

---

## 8. 实现分期

| 阶段      | 范围                                               | 理由                                              |
|---------|--------------------------------------------------|-------------------------------------------------|
| **MVP** | 统一引擎 + **L2 反向剔除**（单矩形/横线/竖线）+ L1 标准提取 + 分离与坍缩导出 | 差异化价值必须在第一个版本就成立。L1 是顺带产物（把极性设为 keep 即可），几乎零成本。 |
| **v2**  | **L3 网格分割**（含排序与重排合并）+ 配置预设                      | 引擎之上的纯增量开发，独立性强。                                |
| **v3**  | 多矩形并集剔除、基础图像处理、标注图层                              | 标注涉及图层系统，最重，放最后。                                |

> **注意**：L2 先于 L1 设计，但同时发布。设计顺序上以反向剔除为中心（因为它定义了产品），发布顺序上两者一起出（因为 L1 只是同一引擎的另一个极性值，且能降低首次使用门槛）。
---

## 9. 操作配置数据结构参考

```json
{
  "source": { "width": 1920, "height": 1080 },
  "preprocess": [
    { "op": "rotate", "deg": 90 },
    { "op": "flip", "axis": "horizontal" }
  ],
  "cut": {
    "tier": "L2",
    "generator": "rect",
    "params": { "x1": 300, "y1": 200, "x2": 900, "y2": 700 },
    "polarity": "remove"
  },
  "order": { "strategy": "row-major", "reverse": false, "sequence": null },
  "emit": {
    "mode": "merged",
    "merge": {
      "layout": "collapse",
      "cols": null, "rows": null,
      "cellSize": null,
      "padColor": "#00000000"
    },
    "format": "png",
    "naming": "{name}_{index:03d}"
  }
}
```



---

## 10. 工程架构建议：Core / CLI / GUI

### 10.1 拆分原则

采用三层结构：

```text
project/
├── core/     # 纯逻辑，无 GUI 依赖
├── cli/      # 命令行入口
└── gui/      # 桌面前端
```

Core 首先作为**普通静态库 / library module** 被 CLI 和 GUI 引用：

```text
GUI ──────┐
          ├── Core
CLI ──────┘
```

**不要**提前引入 DLL / C ABI / 版本兼容 / 内存所有权等复杂问题。等 Core 稳定后，再考虑：`libCore`、`Core CLI`、`Core C API`、`Python binding`。

### 10.2 Core API 设计原则

API 应表达真正模型，而不是把所有操作都叫 `crop`。建议概念：

```text
Image
Region
RegionSet
Grid
Selection
Sequence
Composition
```

概念流程：

```text
Image image;

RegionSet regions = split(image, grid);

Selection selection = select(regions, ...);

RegionSet result = remove(regions, selection);

Composition composition = compose(result, sequence, layout);

exportImage(composition, output);
```

三种模式即：

- **标准提取**：`split → select → keep → export`
- **反向剔除**：`split → select → remove → export`
- **网格重组**：`grid → select → sequence → compose → export`

### 10.3 Rust 工程目录参考

```text
image-tool/
├── crates/
│   ├── image-tool-core/          # 纯逻辑，无 GUI 依赖
│   │   ├── src/
│   │   │   ├── lib.rs            # 公开 API
│   │   │   ├── geometry.rs       # 坐标、矩形、切割线
│   │   │   ├── extract.rs        # 标准提取模式
│   │   │   ├── erase.rs          # 反向剔除模式
│   │   │   ├── grid.rs           # 网格分割模式
│   │   │   ├── export.rs         # 导出/合成逻辑
│   │   │   └── image_io.rs       # 图像读写封装
│   │   └── Cargo.toml
│   │
│   ├── image-tool-cli/           # CLI 入口
│   │   ├── src/main.rs           # clap 命令行解析
│   │   └── Cargo.toml
│   │
│   └── image-tool-ffi/           # C ABI 导出（稳定后再加）
│       ├── src/lib.rs
│       └── Cargo.toml
│
├── src-tauri/                    # Tauri 后端（若选 Tauri）
│   ├── src/main.rs
│   └── Cargo.toml
│
├── src/                          # 前端
│   ├── components/
│   ├── canvas/
│   └── stores/
│
└── package.json
```

### 10.4 CLI 示例

```bash
# 反向剔除：删除中间矩形，导出四角为多张图
image-tool erase --input photo.jpg --x1 100 --y1 100 --x2 300 --y2 250 \
  --output-dir ./out/ --format png

# 网格分割：网格，保留 (0,0)(0,2)(2,0)(2,2)，按横优先排序合成单图
image-tool grid --input photo.jpg --grid 100,100,200,150 \
  --keep 0,0 --keep 0,2 --keep 2,0 --keep 2,2 \
  --sort row-major --compose --canvas 800x600 --output result.png
```
