# 安全策略（Security Policy）

## 支持的版本

| 版本                  | 支持          |
|---------------------|-------------|
| `main` 分支（开发版）      | ✅ 接受安全报告    |
| `v1.0.0`（最新正式发布）    | ✅ 接受安全报告    |

GitHub Releases 已启用（正式发布见 Releases 页）；安全修复将随下一个版本发布，
通常仅最新正式版本接受安全报告。

## 报告漏洞

发现安全漏洞时，**请勿公开提交 issue**。通过以下任一方式私密报告：

1. **首选**：GitHub 私有安全通告——本仓库 **Security** 页 → **Report a vulnerability**（仅维护者可见，修复完成后随通告公开）
2. 备用：邮件 `jinzetan@foxmail.com`，标题注明 `[Security] ImageDiscropper`

报告请包含：受影响的版本/提交、复现步骤与最小输入样本、影响评估。请勿附带可直接利用的完整攻击代码。

## 处理流程

- 确认后 **7 天内回复**（本项目为单人业余维护，逾期请礼貌跟进）
- 修复在私有分支进行，随安全通告一并公开
- 修复发布后，将通过 GitHub Security Advisory 披露（含 CVE 编号，若适用）

## 范围

**在范围内**：

- 图像解码路径（stb / libwebp）处理恶意构造图像时的越界读写、崩溃与内存安全问题
- 配置 JSON 解析（nlohmann）与 Core 引擎处理任意输入时的同类问题
- CLI / GUI 由输入触发的安全缺陷

**不在范围内**：

- 依赖库自身已公开的上游漏洞（请直接报告给 stb / libwebp / nlohmann-json / Qt 上游，或提交 issue 跟踪升级）
- 需要本机访问权限的威胁模型之外的问题
- 不构成安全边界的一般功能性缺陷（请走普通 issue）
