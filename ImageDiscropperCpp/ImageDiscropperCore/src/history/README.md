# src/history

**目录作用**：`include/history` 下模板类的实现补充。

**分块依据**：`HistoryManager<T>` 是纯模板类，实现全部放在头文件中。
本目录仅保留一个空翻译单元，作为将来添加非模板辅助函数（如日志、序列化）的扩展落点。

| 文件                    | 职责                    |
|-----------------------|-----------------------|
| `history_manager.cpp` | 空翻译单元占位（供将来扩展非模板辅助函数） |
